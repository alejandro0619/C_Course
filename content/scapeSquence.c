#include <stdio.h>

int main() {
  /*
    Consists of a backslash followed by a letter or combination of digits.
  */

  printf(
    "\"First\" line\nSecond line\t spaced to the right by a tab"
    );
  return 0;
}